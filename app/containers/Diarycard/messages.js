/*
 * Diarycard Messages
 *
 * This contains all the text for the Diarycard container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Diarycard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Diarycard container!',
  },
});
