/*
 * Temp Messages
 *
 * This contains all the text for the Temp container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Temp';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Temp container!',
  },
});
