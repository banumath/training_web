/*
 * DiaryHome Messages
 *
 * This contains all the text for the DiaryHome component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DiaryHome';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DiaryHome component!',
  },
});
